- name: Creating user
  hosts: localhost
  tasks:
  - name: to check the user is exist or not
    ansible.builtin.command: id raja
    register: output
    ignore_errors: true
  
  - name: creating user
    ansible.builtin.user:
      name: raja
    when: {{output.rc}} != 1
    